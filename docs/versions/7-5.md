# What changed in 7.5

Version 7.5.0 added a planned road system and an inter-city highway network. Both
are **on by default**. A setup that worked on 7.4.12 generates different cities
after the upgrade, with no change to your config and no change to your datapack.

This page documents 7.5.1 for Minecraft 1.20.1, verified against that jar. The same
system is in 8.4.1, 9.5.1 and 10.0.1, where it is identical. See
[The NeoForge line](neoforge.md).

!!! danger "`buildingchance` no longer fills every eligible chunk"
    The mod refuses a building in any chunk the road planner has claimed, and it
    refuses it **before** it rolls `buildingchance`. Setting `buildingchance` to
    `1.0` therefore no longer puts a building in every city chunk. To restore the
    7.4.12 behaviour, set `streetGenerationMode` to `LEGACY`.

## The three keys that decide everything

These are the only new keys most readers need. All three defaults were read from
the field assignments in `LostCityProfile`, not from the list of accepted values.

| Key | Values | Default | Meaning |
|---|---|---|---|
| `streetGenerationMode` | `LEGACY`, `HIERARCHICAL_GRID_V1` | `HIERARCHICAL_GRID_V1` | `LEGACY` generates streets as 7.4.12 does, from the city shape alone. `HIERARCHICAL_GRID_V1` plans a road grid first, then fits buildings into what is left. |
| `highwayGenerationMode` | `LEGACY`, `INTERCITY_NETWORK_V1` | `INTERCITY_NETWORK_V1` | `LEGACY` generates highways on fixed intervals as 7.4.12 does. `INTERCITY_NETWORK_V1` places hubs near likely cities and routes highways between them. |
| `multiBuildingStreetConflict` | `BLOCK_ALL`, `OVERRIDE_MINOR`, `OVERRIDE_ALL` | `OVERRIDE_MINOR` | Decides which planned roads stop a multi-building. See the table below. |

### Restoring 7.4.12 behaviour

Set both modes to `LEGACY` in `config/lostcities/common.toml`:

```toml
streetGenerationMode = "LEGACY"
highwayGenerationMode = "LEGACY"
```

`multiBuildingStreetConflict` then has no effect, because there are no planned
roads for a multi-building to conflict with.

### How `multiBuildingStreetConflict` resolves

The mod asks the value whether a given planned road type blocks the
multi-building. The answer is exact:

| Value | Blocks on `PRIMARY` | Blocks on `SECONDARY` | Blocks on `TERTIARY` | Blocks on `NONE` |
|---|---|---|---|---|
| `BLOCK_ALL` | Yes | Yes | Yes | No |
| `OVERRIDE_MINOR` | Yes | No | No | No |
| `OVERRIDE_ALL` | No | No | No | No |

With the default `OVERRIDE_MINOR`, a multi-building cuts through secondary and
tertiary roads, and a primary road still stops it.

An unrecognised value throws `IllegalArgumentException`. The mod uppercases the
string first, so `override_minor` is accepted.

## The mode is stored per world, not per config

The two mode keys set what a **new** world gets. The mod writes the chosen mode
into the world's saved data and reads it back on every later load.

| Situation | Result |
|---|---|
| New world | Takes the mode from the config key. |
| Existing world | Keeps the mode it was created with. Changing the config key does nothing. |
| Saved mode is not a recognised name | Falls back to `LEGACY` and logs `Unknown persisted street mode '<name>' for <dimension>; using LEGACY`. |

The same applies to `highwayGenerationMode`, with the message
`Unknown persisted highway mode '<name>' for <dimension>; using LEGACY`.

!!! warning "Changing the config key does not convert an existing world"
    The mod reads the persisted mode, not the config key, for any world that
    already exists. To change the road system of a world you already generated,
    you must start a new world.

## The road hierarchy

`HIERARCHICAL_GRID_V1` classifies every city chunk as one of four planned road
types. The type decides whether a building may stand there.

| Type | What it is | Building allowed |
|---|---|---|
| `PRIMARY` | The main corridors. Spacing is set by `primaryRoadSpacingX` and `primaryRoadSpacingZ`. | No |
| `SECONDARY` | Roads inside a primary block. Count is set by the four `secondaryRoadCount` keys. | No |
| `TERTIARY` | Short access roads into a subdivided block. | No |
| `NONE` | Not a planned road. | Yes, subject to `buildingchance` |

### The order the mod decides in

For each city chunk, `checkBuildingPossibility` runs these tests in this order and
returns at the first one that matches:

1. A predefined building is at this chunk. **Building.**
2. A predefined street is at this chunk. **No building.**
3. The chunk is part of a multi-building. **Building.**
4. The planned road type is not `NONE`. **No building.**
5. The `buildingchance` roll fails. **No building.**
6. Highway and level tests follow.

Step 4 sits above step 5. That is the whole reason `buildingchance` stopped
behaving as it did in 7.4.12.

Note that step 3 sits above step 4. A multi-building that survived the
`multiBuildingStreetConflict` test wins against a planned road.

## New profile keys

Version 7.5.1 adds 29 profile keys and removes none. The three above are covered
already. The remaining 26 all tune the two planners.

The defaults and the ranges below are read from the registration calls, so they are
exact. **The meanings are the mod's own descriptions**, not behaviour this wiki has
traced to its consumer. Treat them as the author's intent.

| Key | Type | Default | Range | The mod's description |
|---|---|---|---|---|
| `primaryRoadSpacingX` | Int | `8` | `8` to `128` | Horizontal chunk spacing of candidate north/south primary-road corridors |
| `primaryRoadSpacingZ` | Int | `8` | `8` to `128` | Vertical chunk spacing of candidate east/west primary-road corridors |
| `primaryRoadOptionalChance` | Float | `0.45` | `0.0` to `1.0` | Chance that a non-forced candidate primary-road corridor is enabled |
| `primaryRoadForceEvery` | Int | `4` | `1` to `16` | Force every Nth candidate primary-road corridor to cap the maximum gap |
| `secondaryRoadMinCountX` | Int | `0` | `0` to `128` | Minimum internal north/south secondary roads in a primary block |
| `secondaryRoadMaxCountX` | Int | `2` | `0` to `128` | Maximum internal north/south secondary roads in a primary block |
| `secondaryRoadMinCountZ` | Int | `0` | `0` to `128` | Minimum internal east/west secondary roads in a primary block |
| `secondaryRoadMaxCountZ` | Int | `2` | `0` to `128` | Maximum internal east/west secondary roads in a primary block |
| `minimumRoadSeparation` | Int | `4` | `2` to `32` | Minimum chunk distance between parallel secondary roads |
| `minimumRoadEdgeDistance` | Int | `3` | `2` to `32` | Minimum chunk distance between a secondary road and its bounding primary road |
| `tertiaryRoadChance` | Float | `0.4` | `0.0` to `1.0` | Chance for a subdivided block to contain one short tertiary access road |
| `tertiaryRoadMinLength` | Int | `2` | `1` to `16` | Minimum tertiary access-road length in chunks |
| `tertiaryRoadMaxLength` | Int | `5` | `1` to `32` | Maximum tertiary access-road length in chunks |
| `openLotParkChance` | Float | `0.8` | `0.0` to `1.0` | The chance that a hierarchical open lot gets a park part |
| `plannedPrimaryBridgeChance` | Float | `1.0` | `0.0` to `1.0` | Chance for an entire eligible hierarchical primary-road water crossing to become a bridge |
| `plannedPrimaryBridgeMaxLength` | Int | `12` | `1` to `64` | Maximum water-gap length in chunks for a hierarchical primary-road bridge |
| `highwayPlanningCellSize` | Int | `128` | `32` to `512` | Size in chunks of one INTERCITY_NETWORK_V1 hub-planning cell |
| `highwayHubSampleSpacing` | Int | `16` | `1` to `512` | Chunk spacing of deterministic city-potential samples inside a highway planning cell |
| `highwayHubMinimumPotential` | Float | `0.2` | `0.0` to `1.0` | Minimum height- and biome-adjusted city-potential score required to create a highway hub |
| `highwayHubSearchRadiusCells` | Int | `2` | `0` to `8` | Bounded planning-cell radius in which a highway hub considers other hubs |
| `highwayMinimumHubDistance` | Int | `64` | `0` to `4096` | Minimum Euclidean chunk distance between connected highway hubs |
| `highwayMaximumHubDistance` | Int | `320` | `0` to `4096` | Maximum Euclidean chunk distance between connected highway hubs |
| `highwayMaximumConnectionsPerHub` | Int | `2` | `1` to `8` | Maximum accepted INTERCITY_NETWORK_V1 connections incident to one hub |
| `highwayMinimumRouteLength` | Int | `40` | `0` to `4096` | Minimum Manhattan length in chunks of an inter-city highway route |
| `highwayRouteCityPenalty` | Float | `1.0` | `0.0` to `1000.0` | Weight applied to approximate city-potential samples when choosing an L-route bend |
| `highwayNetworkLevel` | Int | `0` | `0` to `32` | Fixed highway level used when highwayLevelFromCities is 4 |

## Existing keys that changed

Three keys that already existed behave differently in 7.5.1. No key was removed.

### `highwayLevelFromCities` changed its default

| | 7.4.12 | 7.5.1 |
|---|---|---|
| Range | `0` to `3` | `0` to `4` |
| Default | `0` | `3` |

Two separate changes, and the default is the one that affects you. On 7.4.12 a
highway took its height from one endpoint city. On 7.5.1 it takes the average
height of both, unless you set the key back to `0`.

The new value `4` makes the mod use the fixed `highwayNetworkLevel` instead of
deriving the level from the endpoint cities.

The description of value `0` also changed, from "take height from top-left city" to
"take height from the first endpoint city".

### The spawn search changed

Neither key changed its default or its range. What the numbers mean changed.

| Key | 7.4.12 | 7.5.1 |
|---|---|---|
| `spawnCheckAttempts` | A count of attempts. | A count of chunks to check. |
| `spawnCheckRadius` | The start radius. The mod widened it by a fixed 100 on failure. | The start radius. The mod spirals outward from the world centre and widens by `spawnRadiusIncrease`. |

If you raised `spawnCheckAttempts` on 7.4.12 to work around a spawn failure, the
same number now means a different amount of searching.

### These ranges are enforced

Unlike the building keys, these are validated. The mod rejects combinations that
cannot work, with messages such as:

- `Secondary road minimum counts cannot exceed maximum counts`
- `highwayMinimumHubDistance cannot exceed highwayMaximumHubDistance`
- `highwayHubSampleSpacing cannot exceed highwayPlanningCellSize`
- `tertiaryRoadMinLength cannot exceed tertiaryRoadMaxLength`
- `Primary road candidate spacing must be between 8 and 128 chunks`
- `Road separation and edge distance must be between 2 and 32 chunks`
- `Secondary road counts must be between 0 and 128`

## New datapack keys

Version 7.5.1 adds 7 datapack keys, removes none, and changes no key from optional
to required or back. Every 7.4.12 datapack still loads.

| Asset | Key | Notes |
|---|---|---|
| City style | `profile_overrides` | An object holding per-city-style profile values. |
| City style, inside `profile_overrides` | `openLotParkChance` | The only key the object accepts in 7.5.1. |
| Selectors | `largebridges` | Selector list, alongside the existing bridge selectors. |
| Street settings | `largeparts` | Part names for the wider planned roads. |
| Street settings | `tertiaryparts` | Part names for tertiary access roads. |
| Scattered building | `supportpart` | Part name used underneath a scattered building. |
| Scattered building | `clearhighwayrailing` | Whether the scattered building clears highway railings. |

`profile_overrides` on a city style is new in kind, not only in name. Before 7.5.1
a city style could not override a profile value at all. The mod resolves the pair
through `EffectiveCitySettings`, and `buildingchance` is read through that path,
so a city style can now carry its own building chance.

## What is new inside the mod

For orientation only. 7.5.1 adds 67 classes over 7.4.12 and removes one.

| Package | Classes | Purpose |
|---|---|---|
| `worldgen.highway` | 24 | Hub placement, route planning, persistence of the hub graph. |
| `worldgen.street` | 12 | The planned road grid, bridge planning, road types. |
| `worldgen.gen` | 12 to 22 | Grew, including scattered-building placement. |

The only removal is `LostCityTerrainFeature$AvoidChunk`.
